$("#flash_notice").html("<%= escape_javascript(flash.now[:notice]) %>");
<% if @user_file.nil? %>
  $("#userfile_forms").append("<%= escape_javascript(render :partial=>'form') %>");
<% else %>
  $("#userfile_added").append("<%= escape_javascript(render(@user_file))%>");
<% end %>

